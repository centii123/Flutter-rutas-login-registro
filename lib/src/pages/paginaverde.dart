import 'package:flutter/material.dart';

class Paginaverde extends StatelessWidget {
  const Paginaverde({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("pagina verde");
  }
}